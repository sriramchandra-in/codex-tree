<!-- codex-tree: generation=4, delta_count=4, format_version=0.1.0 -->


# Cursor: codex-tree digest

These files are the same **structural summary** as `.codex-tree/claude/*.md`, with guidance for **Cursor** (Chat, Composer, Agent).

## Pick a level

| File | Use when |
|------|----------|
| `l1.md` | Quick orientation, small edits, locating entry points |
| `l2.md` | Feature work — public APIs, imports, intent **patterns** |
| `l3.md` | Refactors / architecture — all symbols, decisions, import/export detail |

## Attach in Cursor

- Use **@** → **Files** → `.codex-tree/cursor/l1.md` (or `l2` / `l3`).
- Prefer **l1 first**; escalate only if the task needs more of the module graph or full symbol lists.
- Optional: a [`.cursor/rules`](https://docs.cursor.com/context/rules-for-ai) entry can point agents at `l2.md` as default repo context.

## Beyond this digest

- Per-file indices: `.codex-tree/modules/<path>/index.json`
- Source of truth remains the repository; use search / read tools for code not spelled out here.

## Intent JSON (when generated)

- `intent/patterns.json` and `intent/decisions.json` are rolled into **Design Patterns** (l2+) and **Design Decisions** (l3).

---

# Project Overview

**codex-tree** — generation 4, 4 incremental updates

---

## Quick Stats

| Metric | Value |
|--------|-------|
| Files | 33 |
| Symbols | 230 |
| Lines of code | 5757 |
| Languages | rust |

---

## Architecture

- **crates** — 3 children, 227 symbols

---

## Entry Points

- `crates/codex-cli/src/main.rs`

---

## Key Public Types

- `pub struct InitOptions` _(in `crates/codex-cli/src/commands/init.rs`)_ — Boolean flags for `codex-tree init` (keeps `run` under Clippy's argument limit).
- `pub struct RustAdapter` _(in `crates/codex-parser/src/languages/rust.rs`)_ — Language adapter for Rust source files.
- `pub struct ParserRegistry` _(in `crates/codex-parser/src/registry.rs`)_ — Maps file extensions to language adapters.
- `pub trait LanguageAdapter: Send + Sync` _(in `crates/codex-parser/src/language.rs`)_ — The core abstraction for language support — every language plugin implements this.
- `pub struct Analyzer` _(in `crates/codex-analyzer/src/analyzer.rs`)_ — Orchestrates intent analysis: cache lookup → prompt build → API call →

- `pub struct ClaudeLayerOutput` _(in `crates/codex-analyzer/src/types.rs`)_ — The three progressive-detail markdown documents for `.
- `pub struct CursorLayerOutput` _(in `crates/codex-analyzer/src/types.rs`)_ — The three progressive-detail markdown documents for `.
- `pub struct ClaudeClient` _(in `crates/codex-analyzer/src/client.rs`)_ — Stateful Claude API client.
- `pub enum CliError` _(in `crates/codex-cli/src/error.rs`)_
- `pub enum SymbolKind` _(in `crates/codex-parser/src/types.rs`)_

---

## Module Details

### `crates/codex-cli/src/main.rs`

Language: **rust** | Symbols: 8 | Public API: 0

_No public symbols._

### `crates/codex-cli/src/utils.rs`

Language: **rust** | Symbols: 7 | Public API: 5

- `pub fn find_git_root(start: &Path) -> Option<PathBuf>`
- `pub fn read_all_modules(codex_tree_dir: &Path) -> Result<Vec<ModuleIndex>>`
- `pub fn format_size(bytes: u64) -> String`
- `pub fn calculate_dir_size(path: &Path) -> std::io::Result<u64>`
- `pub fn current_utc_iso8601() -> String`

### `crates/codex-cli/src/compaction.rs`

Language: **rust** | Symbols: 8 | Public API: 4

- `pub fn should_compact(version: &TreeVersion) -> bool`
- `pub fn read_deltas(codex_tree_dir: &Path) -> Result<Vec<Delta>>`
- `pub fn calculate_delta_size(codex_tree_dir: &Path) -> Result<u64>`
- `pub fn compact(codex_tree_dir: &Path) -> Result<()>`

### `crates/codex-cli/src/error.rs`

Language: **rust** | Symbols: 2 | Public API: 2

- `pub enum CliError`
- `pub type Result<T> = std::result::Result<T, CliError>`

### `crates/codex-cli/src/commands/report.rs`

Language: **rust** | Symbols: 10 | Public API: 1

- `pub fn run(path: &Path, format: &str, _benchmark: bool, _verbose: bool, quiet: bool) -> Result<()>`

### `crates/codex-cli/src/commands/regen.rs`

Language: **rust** | Symbols: 4 | Public API: 1

- `pub fn run(
    path: &Path,
    no_intent: bool,
    no_claude: bool,
    no_cursor: bool,
    _languages: Option<&str>,
    verbose: bool,
    quiet: bool,
) -> Result<()>`

### `crates/codex-cli/src/commands/mod.rs`

Language: **rust** | Symbols: 5 | Public API: 5

- `pub mod check`
- `pub mod init`
- `pub mod regen`
- `pub mod report`
- `pub mod update`

### `crates/codex-cli/src/commands/init.rs`

Language: **rust** | Symbols: 5 | Public API: 2

- `pub struct InitOptions`
- `pub fn run(path: &Path, options: InitOptions, _languages: Option<&str>) -> Result<()>`

### `crates/codex-cli/src/commands/check.rs`

Language: **rust** | Symbols: 8 | Public API: 1

- `pub fn run(
    path: &Path,
    format: &str,
    fail_if_stale: bool,
    _verbose: bool,
    quiet: bool,
) -> Result<()>`

### `crates/codex-cli/src/commands/update.rs`

Language: **rust** | Symbols: 6 | Public API: 1

- `pub fn run(
    path: &Path,
    no_intent: bool,
    no_claude: bool,
    no_cursor: bool,
    no_compact: bool,
    verbose: bool,
    quiet: bool,
) -> Result<()>`

### `crates/codex-cli/src/git.rs`

Language: **rust** | Symbols: 6 | Public API: 5

- `pub fn get_head_commit(repo_root: &Path) -> Result<String>`
- `pub fn get_changed_files(repo_root: &Path, since_commit: &str) -> Result<Vec<String>>`
- `pub fn get_uncommitted_changes(repo_root: &Path) -> Result<Vec<String>>`
- `pub fn is_shallow_clone(repo_root: &Path) -> bool`
- `pub fn get_commit_date(repo_root: &Path, commit: &str) -> Result<String>`

### `crates/codex-parser/tests/rust_parsing.rs`

Language: **rust** | Symbols: 10 | Public API: 0

_No public symbols._

### `crates/codex-parser/tests/integration.rs`

Language: **rust** | Symbols: 2 | Public API: 0

_No public symbols._

### `crates/codex-parser/src/serializer.rs`

Language: **rust** | Symbols: 5 | Public API: 3

- `pub fn write_tree(
    output_dir: &Path,
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
) -> Result<()>`
- `pub fn create_initial_version(stats: &TreeStats) -> TreeVersion`
- `pub fn compute_stats(tree: &TreeStructure, modules: &[ModuleIndex]) -> TreeStats`

### `crates/codex-parser/src/diff.rs`

Language: **rust** | Symbols: 8 | Public API: 1

- `pub fn diff_modules(old: &ModuleIndex, new: &ModuleIndex) -> DeltaOperation`

### `crates/codex-parser/src/types.rs`

Language: **rust** | Symbols: 16 | Public API: 14

- `pub enum SymbolKind`
- `pub enum Visibility`
- `pub struct Span`
- `pub struct Symbol`
- `pub struct Import`
- `pub struct Export`
- `pub struct ModuleIndex`
- `pub enum TreeEntry`
- `pub struct TreeStructure`
- `pub struct TreeVersion`
- `pub struct TreeVersionNumber`
- `pub struct TreeStats`
- `pub enum DeltaOperation`
- `pub struct Delta`

### `crates/codex-parser/src/error.rs`

Language: **rust** | Symbols: 2 | Public API: 2

- `pub enum ParserError`
- `pub type Result<T> = std::result::Result<T, ParserError>`

### `crates/codex-parser/src/lib.rs`

Language: **rust** | Symbols: 8 | Public API: 8

- `pub mod error`
- `pub mod language`
- `pub mod languages`
- `pub mod registry`
- `pub mod types`
- `pub mod diff`
- `pub mod parser`
- `pub mod serializer`

### `crates/codex-parser/src/languages/mod.rs`

Language: **rust** | Symbols: 1 | Public API: 1

- `pub mod rust`

### `crates/codex-parser/src/languages/rust.rs`

Language: **rust** | Symbols: 10 | Public API: 1

- `pub struct RustAdapter`

### `crates/codex-parser/src/parser.rs`

Language: **rust** | Symbols: 5 | Public API: 2

- `pub fn parse_file(path: &Path, source: &[u8], registry: &ParserRegistry) -> Result<ModuleIndex>`
- `pub fn parse_directory(
    root: &Path,
    registry: &ParserRegistry,
    exclude_dirs: &[&str],
) -> Result<(TreeStructure, Vec<ModuleIndex>)>`

### `crates/codex-parser/src/registry.rs`

Language: **rust** | Symbols: 9 | Public API: 6

- `pub struct ParserRegistry`
- `pub fn new() -> Self`
- `pub fn register(&mut self, adapter: Box<dyn LanguageAdapter>)`
- `pub fn get_adapter(&self, extension: &str) -> Option<&dyn LanguageAdapter>`
- `pub fn languages(&self) -> Vec<&str>`
- `pub fn with_defaults() -> Self`

### `crates/codex-parser/src/language.rs`

Language: **rust** | Symbols: 1 | Public API: 1

- `pub trait LanguageAdapter: Send + Sync`

### `crates/codex-analyzer/src/analyzer.rs`

Language: **rust** | Symbols: 5 | Public API: 4

- `pub struct Analyzer`
- `pub fn new(client: ClaudeClient) -> Self`
- `pub async fn analyze(
        &mut self,
        codex_tree_dir: &Path,
        modules: &[ModuleIndex],
    ) -> Result<IntentOutput>`
- `pub fn write_intent(codex_tree_dir: &Path, output: &IntentOutput) -> Result<()>`

### `crates/codex-analyzer/src/types.rs`

Language: **rust** | Symbols: 9 | Public API: 9

- `pub enum Provenance`
- `pub struct Anchor`
- `pub struct Decision`
- `pub struct Pattern`
- `pub struct DecisionsDocument`
- `pub struct PatternsDocument`
- `pub struct IntentOutput`
- `pub struct ClaudeLayerOutput`
- `pub struct CursorLayerOutput`

### `crates/codex-analyzer/src/error.rs`

Language: **rust** | Symbols: 2 | Public API: 2

- `pub enum AnalyzerError`
- `pub type Result<T> = std::result::Result<T, AnalyzerError>`

### `crates/codex-analyzer/src/lib.rs`

Language: **rust** | Symbols: 9 | Public API: 9

- `pub mod analyzer`
- `pub mod cache`
- `pub mod claude_layer`
- `pub mod client`
- `pub mod cursor_layer`
- `pub mod error`
- `pub mod prompts`
- `pub mod response`
- `pub mod types`

### `crates/codex-analyzer/src/cache.rs`

Language: **rust** | Symbols: 4 | Public API: 3

- `pub fn cache_key(modules: &[ModuleIndex]) -> String`
- `pub fn load_cached(codex_tree_dir: &Path, key: &str) -> Option<IntentOutput>`
- `pub fn save_to_cache(
    codex_tree_dir: &Path,
    key: &str,
    output: &IntentOutput,
) -> std::io::Result<()>`

### `crates/codex-analyzer/src/claude_layer.rs`

Language: **rust** | Symbols: 20 | Public API: 5

- `pub fn generate_metadata_header(version: &TreeVersion) -> String`
- `pub fn generate_l1(tree: &TreeStructure, modules: &[ModuleIndex], version: &TreeVersion) -> String`
- `pub fn generate_l2(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> String`
- `pub fn generate_l3(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> String`
- `pub fn write_claude_layer(output_dir: &Path, l1: &str, l2: &str, l3: &str) -> std::io::Result<()>`

### `crates/codex-analyzer/src/client.rs`

Language: **rust** | Symbols: 10 | Public API: 4

- `pub struct ClaudeClient`
- `pub fn from_env() -> Result<Self>`
- `pub fn model(&self) -> &str`
- `pub async fn send_message(
        &mut self,
        system: &str,
        user_content: &str,
        max_tokens: usize,
    ) -> Result<String>`

### `crates/codex-analyzer/src/prompts.rs`

Language: **rust** | Symbols: 4 | Public API: 3

- `pub fn intent_system_prompt() -> String`
- `pub fn intent_user_prompt(modules: &[ModuleIndex]) -> String`
- `pub fn format_module_summary(module: &ModuleIndex) -> String`

### `crates/codex-analyzer/src/response.rs`

Language: **rust** | Symbols: 8 | Public API: 1

- `pub fn parse_intent_response(raw: &str, model: &str) -> Result<IntentOutput>`

### `crates/codex-analyzer/src/cursor_layer.rs`

Language: **rust** | Symbols: 13 | Public API: 5

- `pub fn generate_cursor_l1(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
) -> String`
- `pub fn generate_cursor_l2(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> String`
- `pub fn generate_cursor_l3(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> String`
- `pub fn generate_cursor_layer_output(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> crate::types::CursorLayerOutput`
- `pub fn write_cursor_layer(output_dir: &Path, l1: &str, l2: &str, l3: &str) -> std::io::Result<()>`

---

## Dependency Graph

**`crates/codex-cli/src/main.rs`** imports:
- `std::path::Path` (use)
- `clap::{Parser, Subcommand}` (use)
- `colored::Colorize` (use)

**`crates/codex-cli/src/utils.rs`** imports:
- `std::fs` (use)
- `std::path::{Path, PathBuf}` (use)
- `codex_parser::types::ModuleIndex` (use)
- `crate::error::Result` (use)
- `std::time::{SystemTime, UNIX_EPOCH}` (use)

**`crates/codex-cli/src/compaction.rs`** imports:
- `std::fs` (use)
- `std::path::Path` (use)
- `codex_parser::types::{
    Delta, DeltaOperation, ModuleIndex, TreeEntry, TreeStructure, TreeVersion,
}` (use)
- `crate::error::Result` (use)

**`crates/codex-cli/src/error.rs`** imports:
- `std::path::PathBuf` (use)

**`crates/codex-cli/src/commands/report.rs`** imports:
- `std::fs` (use)
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `codex_parser::types::{ModuleIndex, TreeVersion}` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::utils::{calculate_dir_size, find_git_root, format_size, read_all_modules}` (use)

**`crates/codex-cli/src/commands/regen.rs`** imports:
- `std::fs` (use)
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `codex_parser::parser::parse_directory` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `codex_parser::serializer::{compute_stats, write_tree}` (use)
- `codex_parser::types::{TreeVersion, TreeVersionNumber}` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::git` (use)
- `crate::utils::{calculate_dir_size, current_utc_iso8601, find_git_root, format_size}` (use)
- `codex_analyzer::analyzer::{write_intent, Analyzer}` (use)
- `codex_analyzer::client::ClaudeClient` (use)
- `codex_analyzer::claude_layer` (use)
- `codex_analyzer::cursor_layer` (use)

**`crates/codex-cli/src/commands/init.rs`** imports:
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `codex_parser::parser::parse_directory` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `codex_parser::serializer::{compute_stats, create_initial_version, write_tree}` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::git` (use)
- `crate::utils::{calculate_dir_size, find_git_root, format_size}` (use)
- `codex_analyzer::analyzer::{write_intent, Analyzer}` (use)
- `codex_analyzer::client::ClaudeClient` (use)
- `codex_analyzer::claude_layer` (use)
- `codex_analyzer::cursor_layer` (use)

**`crates/codex-cli/src/commands/check.rs`** imports:
- `std::fs` (use)
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `sha2::{Digest, Sha256}` (use)
- `codex_parser::types::TreeVersion` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::git` (use)
- `crate::utils::{find_git_root, read_all_modules}` (use)

**`crates/codex-cli/src/commands/update.rs`** imports:
- `std::fs` (use)
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `codex_parser::diff::diff_modules` (use)
- `codex_parser::parser::parse_file` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `codex_parser::serializer::compute_stats` (use)
- `codex_parser::types::{Delta, DeltaOperation, ModuleIndex, TreeStructure, TreeVersion}` (use)
- `crate::compaction::{calculate_delta_size, compact, should_compact}` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::git::{get_changed_files, get_commit_date, get_head_commit, get_uncommitted_changes}` (use)
- `crate::utils::{current_utc_iso8601, find_git_root, read_all_modules}` (use)
- `sha2::{Digest, Sha256}` (use)
- `codex_analyzer::analyzer::{write_intent, Analyzer}` (use)
- `codex_analyzer::client::ClaudeClient` (use)
- `codex_analyzer::claude_layer` (use)
- `codex_analyzer::cursor_layer` (use)

**`crates/codex-cli/src/git.rs`** imports:
- `std::path::Path` (use)
- `std::process::Command` (use)
- `crate::error::{CliError, Result}` (use)

**`crates/codex-parser/tests/rust_parsing.rs`** imports:
- `codex_parser::parser::parse_file` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `codex_parser::types::{SymbolKind, Visibility}` (use)
- `std::path::Path` (use)

**`crates/codex-parser/tests/integration.rs`** imports:
- `codex_parser::parser::parse_directory` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `std::path::Path` (use)

**`crates/codex-parser/src/serializer.rs`** imports:
- `std::collections::HashSet` (use)
- `std::fs` (use)
- `std::path::Path` (use)
- `std::time::{SystemTime, UNIX_EPOCH}` (use)
- `crate::error::Result` (use)
- `crate::types::{ModuleIndex, TreeStats, TreeStructure, TreeVersion, TreeVersionNumber}` (use)

**`crates/codex-parser/src/diff.rs`** imports:
- `std::collections::HashSet` (use)
- `crate::types::{DeltaOperation, ModuleIndex}` (use)
- `super::*` (use)
- `crate::types::{Export, Import, ModuleIndex, Span, Symbol, SymbolKind, Visibility}` (use)

**`crates/codex-parser/src/types.rs`** imports:
- `serde::{Deserialize, Serialize}` (use)

**`crates/codex-parser/src/languages/rust.rs`** imports:
- `tree_sitter::Language` (use)
- `crate::language::LanguageAdapter` (use)
- `crate::types::{Import, Span, Symbol, SymbolKind, Visibility}` (use)

**`crates/codex-parser/src/parser.rs`** imports:
- `std::collections::HashSet` (use)
- `std::path::Path` (use)
- `sha2::{Digest, Sha256}` (use)
- `walkdir::WalkDir` (use)
- `crate::error::{ParserError, Result}` (use)
- `crate::registry::ParserRegistry` (use)
- `crate::types::{Export, ModuleIndex, TreeEntry, TreeStructure, Visibility}` (use)

**`crates/codex-parser/src/registry.rs`** imports:
- `std::collections::HashMap` (use)
- `crate::language::LanguageAdapter` (use)

**`crates/codex-parser/src/language.rs`** imports:
- `tree_sitter::Language` (use)
- `crate::types::{Import, Symbol}` (use)

**`crates/codex-analyzer/src/analyzer.rs`** imports:
- `std::fs` (use)
- `std::path::Path` (use)
- `codex_parser::types::ModuleIndex` (use)
- `crate::cache` (use)
- `crate::client::ClaudeClient` (use)
- `crate::error::Result` (use)
- `crate::prompts` (use)
- `crate::response` (use)
- `crate::types::IntentOutput` (use)

**`crates/codex-analyzer/src/types.rs`** imports:
- `serde::{Deserialize, Serialize}` (use)

**`crates/codex-analyzer/src/cache.rs`** imports:
- `std::fs` (use)
- `std::path::Path` (use)
- `sha2::{Digest, Sha256}` (use)
- `codex_parser::types::ModuleIndex` (use)
- `crate::types::IntentOutput` (use)

**`crates/codex-analyzer/src/claude_layer.rs`** imports:
- `std::fmt::Write as FmtWrite` (use)
- `std::fs` (use)
- `std::path::Path` (use)
- `codex_parser::types::{
    ModuleIndex, SymbolKind, TreeEntry, TreeStructure, TreeVersion, Visibility,
}` (use)
- `crate::types::IntentOutput` (use)
- `super::*` (use)
- `codex_parser::types::{
        Export, Import, ModuleIndex, Span, Symbol, SymbolKind, TreeEntry, TreeStats, TreeStructure,
        TreeVersion, TreeVersionNumber, Visibility,
    }` (use)

**`crates/codex-analyzer/src/client.rs`** imports:
- `std::env` (use)
- `reqwest::Client` (use)
- `serde::{Deserialize, Serialize}` (use)
- `crate::error::{AnalyzerError, Result}` (use)

**`crates/codex-analyzer/src/prompts.rs`** imports:
- `codex_parser::types::ModuleIndex` (use)

**`crates/codex-analyzer/src/response.rs`** imports:
- `serde::Deserialize` (use)
- `std::time::{SystemTime, UNIX_EPOCH}` (use)
- `crate::error::{AnalyzerError, Result}` (use)
- `crate::types::{
    Anchor, Decision, DecisionsDocument, IntentOutput, Pattern, PatternsDocument, Provenance,
}` (use)

**`crates/codex-analyzer/src/cursor_layer.rs`** imports:
- `std::fmt::Write as FmtWrite` (use)
- `std::fs` (use)
- `std::path::Path` (use)
- `codex_parser::types::{ModuleIndex, TreeStructure, TreeVersion}` (use)
- `crate::claude_layer` (use)
- `crate::types::IntentOutput` (use)
- `super::*` (use)
- `codex_parser::types::{
        Export, Span, Symbol, SymbolKind, TreeEntry, TreeStats, TreeStructure, TreeVersion,
        TreeVersionNumber, Visibility,
    }` (use)

---

## Design Patterns

_Intent layer not generated._

---

## All Symbols

### `crates/codex-cli/src/main.rs`

- `mod commands` [priv, Module] lines 6-6
- `mod compaction` [priv, Module] lines 7-7
- `mod error` [priv, Module] lines 8-8
- `mod git` [priv, Module] lines 9-9
- `mod utils` [priv, Module] lines 10-10
- `struct Cli` [priv, Struct] lines 15-30
- `enum Commands` [priv, Enum] lines 33-116
- `fn main()` [priv, Function] lines 118-191

### `crates/codex-cli/src/utils.rs`

- `pub fn find_git_root(start: &Path) -> Option<PathBuf>` [pub, Function] lines 10-21
  > /// Walk up the directory tree from `start` looking for a `.git` directory.
  > 
  > /// Returns the directory that *contains* `.git` (i.e. the repo root).
- `pub fn read_all_modules(codex_tree_dir: &Path) -> Result<Vec<ModuleIndex>>` [pub, Function] lines 24-46
  > /// Read all module indexes currently stored on disk.
- `pub fn format_size(bytes: u64) -> String` [pub, Function] lines 49-59
  > /// Format a byte count as a human-readable string: B, KB, or MB.
- `const KB: u64 = 1_024` [priv, Const] lines 50-50
- `const MB: u64 = 1_024 * KB` [priv, Const] lines 51-51
- `pub fn calculate_dir_size(path: &Path) -> std::io::Result<u64>` [pub, Function] lines 62-71
  > /// Recursively sum the sizes of every file under `path`.
- `pub fn current_utc_iso8601() -> String` [pub, Function] lines 74-103
  > /// Current UTC time as ISO-8601 string.

### `crates/codex-cli/src/compaction.rs`

- `const COMPACT_DELTA_COUNT: u32 = 10` [priv, Const] lines 17-17
- `const COMPACT_DELTA_SIZE_BYTES: u64 = 102_400` [priv, Const] lines 18-18
- `pub fn should_compact(version: &TreeVersion) -> bool` [pub, Function] lines 25-28
  > /// Returns `true` if compaction should run.
  > 
  > ///
  > 
  > /// Triggered when either threshold is exceeded (whichever comes first).
- `pub fn read_deltas(codex_tree_dir: &Path) -> Result<Vec<Delta>>` [pub, Function] lines 31-54
  > /// Read and return all delta files in `deltas/`, sorted by sequence number.
- `pub fn calculate_delta_size(codex_tree_dir: &Path) -> Result<u64>` [pub, Function] lines 57-72
  > /// Sum the total file sizes of all JSON files inside `deltas/`.
- `pub fn compact(codex_tree_dir: &Path) -> Result<()>` [pub, Function] lines 82-124
  > /// Apply all pending deltas to the canonical tree and reset the delta state.
  > 
  > ///
  > 
  > /// Steps:
  > 
  > /// 1. Read `version.json`
  > 
  > /// 2. Read all delta files from `deltas/` in sequence order
  > 
  > /// 3. Apply each operation to `modules/` and `tree.json`
  > 
  > /// 4. Delete all delta files
  > 
  > /// 5. Reset `delta_count` and `delta_size_bytes` in `version.json`
- `fn apply_operation(
    codex_tree_dir: &Path,
    op: &DeltaOperation,
    tree: &mut TreeStructure,
) -> Result<()>` [priv, Function] lines 129-179
  > /// Apply a single `DeltaOperation` to the on-disk tree state.
- `fn upsert_tree_entry(tree: &mut TreeStructure, module: &ModuleIndex)` [priv, Function] lines 182-210
  > /// Insert or replace the `TreeEntry::File` for a given `ModuleIndex`.

### `crates/codex-cli/src/error.rs`

- `pub enum CliError` [pub, Enum] lines 4-31
- `pub type Result<T> = std::result::Result<T, CliError>` [pub, TypeAlias] lines 33-33

### `crates/codex-cli/src/commands/report.rs`

- `struct Report` [priv, Struct] lines 18-24
- `struct ReportStats` [priv, Struct] lines 27-33
- `struct TokenEstimate` [priv, Struct] lines 36-61
- `pub fn run(path: &Path, format: &str, _benchmark: bool, _verbose: bool, quiet: bool) -> Result<()>` [pub, Function] lines 65-186
- `fn print_strategy_row(label: &str, tokens: usize, raw_tokens: usize)` [priv, Function] lines 188-200
- `fn estimate_tokens(
    git_root: &Path,
    codex_tree_dir: &Path,
    modules: &[ModuleIndex],
) -> Result<TokenEstimate>` [priv, Function] lines 208-274
  > /// Estimate token counts for raw source vs tree representations.
  > 
  > ///
  > 
  > /// The scaffolding reads all file sizes and computes the byte totals.
  > 
  > /// The core decision is how to convert bytes → tokens for each content type.
- `fn digest_token_delta(codex_tree_dir: &Path, layer: &str) -> (Option<String>, usize)` [priv, Function] lines 277-290
  > /// Prefer `{layer}/l2.md`, then `l1.md`; return (relative path for display, extra tokens).
- `enum ContentKind` [priv, Enum] lines 294-298
- `fn bytes_to_tokens(bytes: usize, kind: ContentKind) -> usize` [priv, Function] lines 314-321
  > /// Convert a byte count to an estimated token count using content-aware ratios.
  > 
  > ///
  > 
  > /// BPE tokenizers (like Claude's) split text into subword units. The average
  > 
  > /// bytes-per-token ratio varies by content type:
  > 
  > ///
  > 
  > /// - **Code** (~3.5 bytes/token): identifiers get split at camelCase/snake_case
  > 
  > ///   boundaries, but keywords and operators are usually single tokens. Indentation
  > 
  > ///   whitespace compresses well (one token per indent level regardless of spaces).
  > 
  > ///
  > 
  > /// - **JSON** (~3.0 bytes/token): more token-dense because of repeated structural
  > 
  > ///   characters (`"`, `{`, `}`, `:`, `,`) that each consume a token, and quoted
  > 
  > ///   keys that tokenize less efficiently than bare identifiers.
  > 
  > ///
  > 
  > /// - **Markdown** (~3.3 bytes/token): mix of prose, headings, and code fences.
- `fn format_number(n: usize) -> String` [priv, Function] lines 325-333

### `crates/codex-cli/src/commands/regen.rs`

- `pub fn run(
    path: &Path,
    no_intent: bool,
    no_claude: bool,
    no_cursor: bool,
    _languages: Option<&str>,
    verbose: bool,
    quiet: bool,
) -> Result<()>` [pub, Function] lines 21-182
- `fn run_intent_analysis(
    codex_tree_dir: &std::path::Path,
    modules: &[codex_parser::types::ModuleIndex],
    quiet: bool,
) -> Result<codex_analyzer::types::IntentOutput>` [priv, Function] lines 187-218
  > /// Run intent analysis via Claude API. Returns None with a warning on failure.
- `fn generate_claude_layer(
    codex_tree_dir: &std::path::Path,
    tree: &codex_parser::types::TreeStructure,
    modules: &[codex_parser::types::ModuleIndex],
    version: &codex_parser::types::TreeVersion,
    intent: Option<&codex_analyzer::types::IntentOutput>,
    quiet: bool,
)` [priv, Function] lines 221-248
  > /// Generate the Claude optimization layer (L1/L2/L3 markdown files).
- `fn generate_cursor_layer(
    codex_tree_dir: &std::path::Path,
    tree: &codex_parser::types::TreeStructure,
    modules: &[codex_parser::types::ModuleIndex],
    version: &codex_parser::types::TreeVersion,
    intent: Option<&codex_analyzer::types::IntentOutput>,
    quiet: bool,
)` [priv, Function] lines 250-277

### `crates/codex-cli/src/commands/mod.rs`

- `pub mod check` [pub, Module] lines 1-1
- `pub mod init` [pub, Module] lines 2-2
- `pub mod regen` [pub, Module] lines 3-3
- `pub mod report` [pub, Module] lines 4-4
- `pub mod update` [pub, Module] lines 5-5

### `crates/codex-cli/src/commands/init.rs`

- `pub struct InitOptions` [pub, Struct] lines 16-23
  > /// Boolean flags for `codex-tree init` (keeps `run` under Clippy's argument limit).
- `pub fn run(path: &Path, options: InitOptions, _languages: Option<&str>) -> Result<()>` [pub, Function] lines 25-193
- `fn run_intent_analysis(
    codex_tree_dir: &std::path::Path,
    modules: &[codex_parser::types::ModuleIndex],
    quiet: bool,
) -> Result<codex_analyzer::types::IntentOutput>` [priv, Function] lines 198-229
  > /// Run intent analysis via Claude API. Returns None with a warning on failure.
- `fn generate_claude_layer(
    codex_tree_dir: &std::path::Path,
    tree: &codex_parser::types::TreeStructure,
    modules: &[codex_parser::types::ModuleIndex],
    version: &codex_parser::types::TreeVersion,
    intent: Option<&codex_analyzer::types::IntentOutput>,
    quiet: bool,
)` [priv, Function] lines 232-259
  > /// Generate the Claude optimization layer (L1/L2/L3 markdown files).
- `fn generate_cursor_layer(
    codex_tree_dir: &std::path::Path,
    tree: &codex_parser::types::TreeStructure,
    modules: &[codex_parser::types::ModuleIndex],
    version: &codex_parser::types::TreeVersion,
    intent: Option<&codex_analyzer::types::IntentOutput>,
    quiet: bool,
)` [priv, Function] lines 262-289
  > /// Generate the Cursor optimization layer (L1/L2/L3 markdown under `cursor/`).

### `crates/codex-cli/src/commands/check.rs`

- `struct CheckReport` [priv, Struct] lines 25-33
- `struct StaleFile` [priv, Struct] lines 36-39
- `enum StaleReason` [priv, Enum] lines 43-50
- `impl std::fmt::Display for StaleReason` [priv, Impl] lines 52-60
- `fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result` [priv, Function] lines 53-59 in `StaleReason`
- `pub fn run(
    path: &Path,
    format: &str,
    fail_if_stale: bool,
    _verbose: bool,
    quiet: bool,
) -> Result<()>` [pub, Function] lines 64-229
- `fn count_commits_between(repo_root: &Path, from: &str, to: &str) -> Result<usize>` [priv, Function] lines 234-253
  > /// Count commits between two SHAs using `git rev-list --count`.
- `fn has_supported_extension(path: &str) -> bool` [priv, Function] lines 256-258
  > /// Quick check if a file path has a supported extension (currently just .rs).

### `crates/codex-cli/src/commands/update.rs`

- `pub fn run(
    path: &Path,
    no_intent: bool,
    no_claude: bool,
    no_cursor: bool,
    no_compact: bool,
    verbose: bool,
    quiet: bool,
) -> Result<()>` [pub, Function] lines 29-282
- `fn update_module_on_disk(codex_tree_dir: &Path, module: &ModuleIndex) -> Result<()>` [priv, Function] lines 287-295
  > /// Write a `ModuleIndex` to `modules/{path}/index.json`.
- `fn detect_changed_by_hash(codex_tree_dir: &Path, git_root: &Path) -> Result<Vec<String>>` [priv, Function] lines 299-346
  > /// When there is no `source_commit` in version.json, detect changed files by
  > 
  > /// comparing the `content_hash` in each module index against the current file.
- `fn run_intent_analysis(
    codex_tree_dir: &std::path::Path,
    modules: &[codex_parser::types::ModuleIndex],
    quiet: bool,
) -> Result<codex_analyzer::types::IntentOutput>` [priv, Function] lines 351-382
  > /// Run intent analysis via Claude API. Returns None with a warning on failure.
- `fn generate_claude_layer(
    codex_tree_dir: &std::path::Path,
    tree: &codex_parser::types::TreeStructure,
    modules: &[codex_parser::types::ModuleIndex],
    version: &codex_parser::types::TreeVersion,
    intent: Option<&codex_analyzer::types::IntentOutput>,
    quiet: bool,
)` [priv, Function] lines 385-412
  > /// Generate the Claude optimization layer (L1/L2/L3 markdown files).
- `fn generate_cursor_layer(
    codex_tree_dir: &std::path::Path,
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&codex_analyzer::types::IntentOutput>,
    quiet: bool,
)` [priv, Function] lines 414-441

### `crates/codex-cli/src/git.rs`

- `fn run_git(repo_root: &Path, args: &[&str], hint: &str) -> Result<String>` [priv, Function] lines 17-36
  > /// Run a git subcommand, return its trimmed stdout as a `String`.
  > 
  > ///
  > 
  > /// Returns `Err(CliError::Git)` if the process fails or exits non-zero.
- `pub fn get_head_commit(repo_root: &Path) -> Result<String>` [pub, Function] lines 41-47
  > /// Return the full SHA-1 of the current HEAD commit.
- `pub fn get_changed_files(repo_root: &Path, since_commit: &str) -> Result<Vec<String>>` [pub, Function] lines 52-65
  > /// Return the list of files changed between `since_commit` and HEAD.
  > 
  > ///
  > 
  > /// Paths are repo-relative and use forward slashes.
- `pub fn get_uncommitted_changes(repo_root: &Path) -> Result<Vec<String>>` [pub, Function] lines 72-108
  > /// Return the list of files with uncommitted changes (modified or added).
  > 
  > ///
  > 
  > /// Uses `git status --porcelain`; each output line starts with two-char status
  > 
  > /// codes.  We collect any file that appears as modified (`M`), added (`A`),
  > 
  > /// renamed (`R`), copied (`C`), or untracked (`?`).
- `pub fn is_shallow_clone(repo_root: &Path) -> bool` [pub, Function] lines 115-117
- `pub fn get_commit_date(repo_root: &Path, commit: &str) -> Result<String>` [pub, Function] lines 120-126
  > /// Return the ISO-8601 commit date for the given commit SHA.

### `crates/codex-parser/tests/rust_parsing.rs`

- `fn registry() -> ParserRegistry` [priv, Function] lines 6-8
- `fn parse_rs(source: &[u8]) -> codex_parser::types::ModuleIndex` [priv, Function] lines 13-17
  > /// Parse `source` bytes as a `fake.rs` file and return the `ModuleIndex`.
- `fn test_parse_function()` [priv, Function] lines 22-38
- `fn test_parse_struct()` [priv, Function] lines 41-64
- `fn test_parse_enum()` [priv, Function] lines 67-79
- `fn test_parse_trait()` [priv, Function] lines 82-94
- `fn test_parse_impl_block()` [priv, Function] lines 97-140
- `fn test_parse_imports()` [priv, Function] lines 143-172
- `fn test_parse_multiple_items()` [priv, Function] lines 175-219
- `fn test_parse_visibility_variants()` [priv, Function] lines 222-252

### `crates/codex-parser/tests/integration.rs`

- `fn test_self_parse()` [priv, Function] lines 12-66
- `fn test_self_parse_full_repo()` [priv, Function] lines 73-113

### `crates/codex-parser/src/serializer.rs`

- `pub fn write_tree(
    output_dir: &Path,
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
) -> Result<()>` [pub, Function] lines 24-57
  > /// Write the full codex-tree output layout under `output_dir`.
  > 
  > ///
  > 
  > /// Directory layout created:
  > 
  > /// ```text
  > 
  > /// {output_dir}/
  > 
  > ///   version.json
  > 
  > ///   tree.json
  > 
  > ///   modules/{relative/path}/index.json   (one per ModuleIndex)
  > 
  > ///   deltas/          (empty, reserved for future incremental deltas)
  > 
  > ///   intent/          (empty, reserved for intent declarations)
  > 
  > ///   claude/          (empty, reserved for Claude-specific digest)
  > 
  > ///   cursor/          (empty, reserved for Cursor-specific digest)
  > 
  > /// ```
- `pub fn create_initial_version(stats: &TreeStats) -> TreeVersion` [pub, Function] lines 64-77
  > /// Construct the initial `TreeVersion` document for a fresh `codex-tree init`.
  > 
  > ///
  > 
  > /// `generated_at` is set to the current UTC time formatted as ISO-8601.
  > 
  > /// `source_commit` is `None` — the CLI layer fills this in after resolving
  > 
  > /// the git HEAD commit.
- `pub fn compute_stats(tree: &TreeStructure, modules: &[ModuleIndex]) -> TreeStats` [pub, Function] lines 83-111
  > /// Derive aggregate `TreeStats` from an already-built tree + module list.
  > 
  > ///
  > 
  > /// `delta_size_bytes` is set to `0`; the CLI layer can update this after
  > 
  > /// writing the output directory if it wants an accurate on-disk size.
- `fn current_utc_iso8601() -> String` [priv, Function] lines 119-132
  > /// Return the current UTC time as an ISO-8601 string, e.g. `"2026-03-27T14:00:00Z"`.
  > 
  > ///
  > 
  > /// We derive this from `std::time::SystemTime` to avoid adding a `chrono`
  > 
  > /// dependency to this crate.
- `fn unix_secs_to_datetime(secs: u64) -> (u64, u64, u64, u64, u64, u64)` [priv, Function] lines 138-160
  > /// Convert a Unix timestamp (seconds) to `(year, month, day, hour, min, sec)`.
  > 
  > ///
  > 
  > /// Uses the proleptic Gregorian calendar algorithm so it works beyond 2038
  > 
  > /// without 32-bit overflow.

### `crates/codex-parser/src/diff.rs`

- `pub fn diff_modules(old: &ModuleIndex, new: &ModuleIndex) -> DeltaOperation` [pub, Function] lines 17-55
  > /// Compare an old and new `ModuleIndex` and return a `DeltaOperation::Modify`
  > 
  > /// that describes what changed at the symbol level.
  > 
  > ///
  > 
  > /// - **Added** — names present in `new` but absent in `old`.
  > 
  > /// - **Removed** — names present in `old` but absent in `new`.
  > 
  > /// - **Modified** — names present in both but with a different `signature`,
  > 
  > ///   `span.start_line`, or `span.end_line`.
- `mod tests` [priv, Module] lines 58-166
- `fn make_symbol(name: &str, sig: &str, start: usize, end: usize) -> Symbol` [priv, Function] lines 62-75
- `fn make_module(path: &str, symbols: Vec<Symbol>) -> ModuleIndex` [priv, Function] lines 77-86
- `fn test_no_changes()` [priv, Function] lines 89-107
- `fn test_symbol_added()` [priv, Function] lines 110-126
- `fn test_symbol_removed()` [priv, Function] lines 129-147
- `fn test_symbol_modified_signature()` [priv, Function] lines 150-165

### `crates/codex-parser/src/types.rs`

- `pub enum SymbolKind` [pub, Enum] lines 16-35
- `pub enum Visibility` [pub, Enum] lines 46-53
- `pub struct Span` [pub, Struct] lines 60-63
- `pub struct Symbol` [pub, Struct] lines 72-85
- `pub struct Import` [pub, Struct] lines 91-96
- `pub struct Export` [pub, Struct] lines 100-103
- `pub struct ModuleIndex` [pub, Struct] lines 111-121
- `pub enum TreeEntry` [pub, Enum] lines 132-147
- `pub struct TreeStructure` [pub, Struct] lines 151-155
- `pub struct TreeVersion` [pub, Struct] lines 161-174
- `pub struct TreeVersionNumber` [pub, Struct] lines 181-184
- `impl std::fmt::Display for TreeVersionNumber` [priv, Impl] lines 186-190
- `fn fmt(&self, f: &mut std::fmt::Formatter<'_>) -> std::fmt::Result` [priv, Function] lines 187-189 in `TreeVersionNumber`
- `pub struct TreeStats` [pub, Struct] lines 194-202
- `pub enum DeltaOperation` [pub, Enum] lines 212-226
- `pub struct Delta` [pub, Struct] lines 233-239

### `crates/codex-parser/src/error.rs`

- `pub enum ParserError` [pub, Enum] lines 6-35
- `pub type Result<T> = std::result::Result<T, ParserError>` [pub, TypeAlias] lines 40-40
  > /// Convenience alias — mirrors Scala's `type Result[A] = Either[ParserError, A]`
  > 
  > /// but using Rust's built-in `Result<T, E>`.  Import this in every module so
  > 
  > /// you can write `Result<Foo>` instead of `std::result::Result<Foo, ParserError>`.

### `crates/codex-parser/src/lib.rs`

- `pub mod error` [pub, Module] lines 10-10
- `pub mod language` [pub, Module] lines 11-11
- `pub mod languages` [pub, Module] lines 12-12
- `pub mod registry` [pub, Module] lines 13-13
- `pub mod types` [pub, Module] lines 14-14
- `pub mod diff` [pub, Module] lines 16-16
- `pub mod parser` [pub, Module] lines 17-17
- `pub mod serializer` [pub, Module] lines 18-18

### `crates/codex-parser/src/languages/mod.rs`

- `pub mod rust` [pub, Module] lines 1-1

### `crates/codex-parser/src/languages/rust.rs`

- `pub struct RustAdapter` [pub, Struct] lines 10-10
  > /// Language adapter for Rust source files.
  > 
  > ///
  > 
  > /// This struct is a unit struct (no fields) — equivalent to a Scala `object`
  > 
  > /// that extends a trait.  It exists only as a handle to call the trait methods.
- `impl LanguageAdapter for RustAdapter` [priv, Impl] lines 12-36
- `fn language_id(&self) -> &str` [priv, Function] lines 13-15 in `LanguageAdapter`
- `fn file_extensions(&self) -> &[&str]` [priv, Function] lines 17-19 in `LanguageAdapter`
- `fn tree_sitter_language(&self) -> Language` [priv, Function] lines 21-23 in `LanguageAdapter`
- `fn extract_symbols(&self, tree: &tree_sitter::Tree, source: &[u8]) -> Vec<Symbol>` [priv, Function] lines 25-29 in `LanguageAdapter`
- `fn extract_imports(&self, tree: &tree_sitter::Tree, source: &[u8]) -> Vec<Import>` [priv, Function] lines 31-35 in `LanguageAdapter`
- `impl RustAdapter` [priv, Impl] lines 38-197
- `fn walk_for_imports(node: tree_sitter::Node, source: &[u8], imports: &mut Vec<Import>)` [priv, Function] lines 43-62 in `RustAdapter`
  > /// Recursively walk the CST collecting `use_declaration` nodes.
  > 
  > ///
  > 
  > /// This is a simple recursive descent — for very large files a cursor-based
  > 
  > /// iterative walk would be more stack-efficient, but this is fine for now.
- `fn walk_for_symbols(
        node: tree_sitter::Node,
        source: &[u8],
        parent: Option<&str>,
        symbols: &mut Vec<Symbol>,
    )` [priv, Function] lines 68-196 in `RustAdapter`
  > /// Recursively walk the CST collecting declaration nodes as `Symbol`s.
  > 
  > ///
  > 
  > /// `parent` carries the name of the enclosing `impl` block so that methods
  > 
  > /// and associated items can record their owner.

### `crates/codex-parser/src/parser.rs`

- `pub fn parse_file(path: &Path, source: &[u8], registry: &ParserRegistry) -> Result<ModuleIndex>` [pub, Function] lines 21-67
  > /// Parse a single source file and return a `ModuleIndex`.
  > 
  > ///
  > 
  > /// Steps:
  > 
  > ///   1. Resolve the file extension and look up a registered `LanguageAdapter`.
  > 
  > ///   2. Build a `tree_sitter::Parser`, set the grammar, and parse the bytes.
  > 
  > ///   3. Delegate symbol and import extraction to the adapter.
  > 
  > ///   4. Derive the public `exports` list from symbols with public visibility.
  > 
  > ///   5. Hash the raw bytes for change-detection.
- `pub fn parse_directory(
    root: &Path,
    registry: &ParserRegistry,
    exclude_dirs: &[&str],
) -> Result<(TreeStructure, Vec<ModuleIndex>)>` [pub, Function] lines 77-147
  > /// Walk a directory tree and parse every supported source file.
  > 
  > ///
  > 
  > /// Returns both a `TreeStructure` (the hierarchy for `tree.json`) and a flat
  > 
  > /// `Vec<ModuleIndex>` (one entry per successfully parsed file).
  > 
  > ///
  > 
  > /// Files whose extension has no registered adapter are silently skipped.
  > 
  > /// Directories listed in `exclude_dirs` (matched by directory name, not full
  > 
  > /// path) are not entered.
- `fn compute_hash(source: &[u8]) -> String` [priv, Function] lines 152-157
  > /// SHA-256 content hash, returned as `"sha256:<hex_lowercase>"`.
- `fn build_tree_structure(root: &Path, modules: &[ModuleIndex]) -> TreeStructure` [priv, Function] lines 162-249
  > /// Convert a flat list of `ModuleIndex` entries into a `TreeStructure` that
  > 
  > /// contains both `TreeEntry::File` entries and aggregate `TreeEntry::Directory`
  > 
  > /// entries.
- `fn entry_path(entry: &TreeEntry) -> &str` [priv, Function] lines 252-257
  > /// Extract the `path` field from any `TreeEntry` variant.

### `crates/codex-parser/src/registry.rs`

- `pub struct ParserRegistry` [pub, Struct] lines 14-18
  > /// Maps file extensions to language adapters.
  > 
  > ///
  > 
  > /// Rust's `Box<dyn LanguageAdapter>` is a *trait object* — a fat pointer
  > 
  > /// (data ptr + vtable ptr) that gives you dynamic dispatch without knowing
  > 
  > /// the concrete type at compile time.  In Scala this is just a value typed
  > 
  > /// as the trait: `val adapter: LanguageAdapter = new RustAdapter`.
  > 
  > ///
  > 
  > /// The `Vec` owns the adapters; the `HashMap` stores indices into it rather
  > 
  > /// than duplicate `Box`es, keeping ownership unambiguous.
- `impl ParserRegistry` [priv, Impl] lines 20-67
- `pub fn new() -> Self` [pub, Function] lines 22-27 in `ParserRegistry`
  > /// Create an empty registry with no adapters registered.
- `pub fn register(&mut self, adapter: Box<dyn LanguageAdapter>)` [pub, Function] lines 33-39 in `ParserRegistry`
  > /// Register a language adapter and claim all its file extensions.
  > 
  > ///
  > 
  > /// If two adapters claim the same extension the last one wins — useful
  > 
  > /// for overriding a built-in with a custom implementation.
- `pub fn get_adapter(&self, extension: &str) -> Option<&dyn LanguageAdapter>` [pub, Function] lines 50-54 in `ParserRegistry`
  > /// Look up an adapter by file extension (without the leading dot).
  > 
  > ///
  > 
  > /// Returns `None` when no adapter is registered for that extension —
  > 
  > /// callers should convert this to `ParserError::UnsupportedLanguage`.
  > 
  > ///
  > 
  > /// The return type `Option<&dyn LanguageAdapter>` is Rust's way of saying
  > 
  > /// "a borrowed reference to some value that implements LanguageAdapter" —
  > 
  > /// equivalent to `Option[LanguageAdapter]` in Scala where the value is a
  > 
  > /// shared reference, not a copy.
- `pub fn languages(&self) -> Vec<&str>` [pub, Function] lines 57-59 in `ParserRegistry`
  > /// Return the language IDs of all registered adapters (in registration order).
- `pub fn with_defaults() -> Self` [pub, Function] lines 62-66 in `ParserRegistry`
  > /// Build a registry pre-loaded with all built-in language adapters.
- `impl Default for ParserRegistry` [priv, Impl] lines 72-76
  > /// `Default` delegates to `with_defaults` so that `ParserRegistry::default()`
  > 
  > /// gives you a fully-configured registry — analogous to a Scala companion
  > 
  > /// `object`'s `apply()` factory method.
- `fn default() -> Self` [priv, Function] lines 73-75 in `Default`

### `crates/codex-parser/src/language.rs`

- `pub trait LanguageAdapter: Send + Sync` [pub, Trait] lines 20-39
  > /// The core abstraction for language support — every language plugin implements this.
  > 
  > ///
  > 
  > /// In Scala this would be:
  > 
  > /// ```scala
  > 
  > /// trait LanguageAdapter {
  > 
  > ///   def languageId: String
  > 
  > ///   def fileExtensions: Seq[String]
  > 
  > ///   def extractSymbols(tree: Tree, source: Array[Byte]): Seq[Symbol]
  > 
  > ///   def extractImports(tree: Tree, source: Array[Byte]): Seq[Import]
  > 
  > /// }
  > 
  > /// ```
  > 
  > ///
  > 
  > /// The `Send + Sync` bounds are the Rust equivalent of "this object is safe to
  > 
  > /// share across threads" — required because the registry may be used from a
  > 
  > /// parallel file-walking task.

### `crates/codex-analyzer/src/analyzer.rs`

- `pub struct Analyzer` [pub, Struct] lines 25-27
  > /// Orchestrates intent analysis: cache lookup → prompt build → API call →
  > 
  > /// response parse → cache write.
- `impl Analyzer` [priv, Impl] lines 29-73
- `pub fn new(client: ClaudeClient) -> Self` [pub, Function] lines 33-35 in `Analyzer`
  > /// Construct an `Analyzer` from an already-configured `ClaudeClient`.
  > 
  > ///
  > 
  > /// In Scala: `new Analyzer(client)` — straightforward constructor injection.
- `pub async fn analyze(
        &mut self,
        codex_tree_dir: &Path,
        modules: &[ModuleIndex],
    ) -> Result<IntentOutput>` [pub, Function] lines 47-72 in `Analyzer`
  > /// Run intent analysis on the given modules.
  > 
  > ///
  > 
  > /// Checks the content-hash cache first; calls the Claude API only on a
  > 
  > /// cache miss.  On a successful API call the result is cached before
  > 
  > /// returning.
  > 
  > ///
  > 
  > /// # Errors
  > 
  > /// Propagates any error from the Claude client (network, budget, API key)
  > 
  > /// or the response parser.  Cache misses and cache write failures are
  > 
  > /// treated as non-fatal and silently ignored.
- `pub fn write_intent(codex_tree_dir: &Path, output: &IntentOutput) -> Result<()>` [pub, Function] lines 84-97
  > /// Write intent output to disk as `intent/decisions.json` and
  > 
  > /// `intent/patterns.json` under `codex_tree_dir`.
  > 
  > ///
  > 
  > /// Creates `intent/` if it doesn't exist.  Both JSON files are pretty-printed
  > 
  > /// for human readability in the repository.
  > 
  > ///
  > 
  > /// In Scala: `writeIntent(dir: Path, output: IntentOutput): IO[Unit]`

### `crates/codex-analyzer/src/types.rs`

- `pub enum Provenance` [pub, Enum] lines 17-26
- `pub struct Anchor` [pub, Struct] lines 35-41
- `pub struct Decision` [pub, Struct] lines 57-65
- `pub struct Pattern` [pub, Struct] lines 74-82
- `pub struct DecisionsDocument` [pub, Struct] lines 88-92
- `pub struct PatternsDocument` [pub, Struct] lines 96-100
- `pub struct IntentOutput` [pub, Struct] lines 106-109
- `pub struct ClaudeLayerOutput` [pub, Struct] lines 117-124
  > /// The three progressive-detail markdown documents for `.codex-tree/claude/`.
  > 
  > ///
  > 
  > /// These are generated locally from tree data + intent, no API call needed.
  > 
  > /// In Scala: `case class ClaudeLayerOutput(l1: String, l2: String, l3: String)`
- `pub struct CursorLayerOutput` [pub, Struct] lines 132-136
  > /// The three progressive-detail markdown documents for `.codex-tree/cursor/`.
  > 
  > ///
  > 
  > /// Same underlying content as [`ClaudeLayerOutput`], plus a Cursor usage preamble.
  > 
  > /// Generated locally from tree data + intent, no API call.

### `crates/codex-analyzer/src/error.rs`

- `pub enum AnalyzerError` [pub, Enum] lines 12-41
- `pub type Result<T> = std::result::Result<T, AnalyzerError>` [pub, TypeAlias] lines 46-46
  > /// Convenience alias — mirrors Scala's `type Result[A] = Either[AnalyzerError, A]`
  > 
  > /// but using Rust's built-in `Result<T, E>`. Import this in every module so
  > 
  > /// you can write `Result<Foo>` instead of `std::result::Result<Foo, AnalyzerError>`.

### `crates/codex-analyzer/src/lib.rs`

- `pub mod analyzer` [pub, Module] lines 14-14
- `pub mod cache` [pub, Module] lines 15-15
- `pub mod claude_layer` [pub, Module] lines 16-16
- `pub mod client` [pub, Module] lines 17-17
- `pub mod cursor_layer` [pub, Module] lines 18-18
- `pub mod error` [pub, Module] lines 19-19
- `pub mod prompts` [pub, Module] lines 20-20
- `pub mod response` [pub, Module] lines 21-21
- `pub mod types` [pub, Module] lines 22-22

### `crates/codex-analyzer/src/cache.rs`

- `pub fn cache_key(modules: &[ModuleIndex]) -> String` [pub, Function] lines 31-43
  > /// Compute a deterministic cache key for a set of modules.
  > 
  > ///
  > 
  > /// Algorithm:
  > 
  > /// 1. Collect all `content_hash` values.
  > 
  > /// 2. Sort them (so key is order-independent — module order may vary).
  > 
  > /// 3. Join with `|` as a separator.
  > 
  > /// 4. SHA-256 hash the joined string.
  > 
  > /// 5. Return lowercase hex.
  > 
  > ///
  > 
  > /// In Scala: `modules.map(_.contentHash).sorted.mkString("|")` then hash.
- `pub fn load_cached(codex_tree_dir: &Path, key: &str) -> Option<IntentOutput>` [pub, Function] lines 53-57
  > /// Try to load a cached `IntentOutput` for the given key.
  > 
  > ///
  > 
  > /// Returns `Some(output)` on a cache hit, `None` on any failure (miss, corrupt
  > 
  > /// JSON, missing file) — the caller always has a safe fallback.
  > 
  > ///
  > 
  > /// Path: `{codex_tree_dir}/intent/.cache/{key}.json`
- `pub fn save_to_cache(
    codex_tree_dir: &Path,
    key: &str,
    output: &IntentOutput,
) -> std::io::Result<()>` [pub, Function] lines 70-87
  > /// Persist an `IntentOutput` to the cache.
  > 
  > ///
  > 
  > /// Creates parent directories if they don't exist — analogous to
  > 
  > /// `Files.createDirectories` in Scala's `java.nio`.
  > 
  > ///
  > 
  > /// Failures are propagated as `std::io::Error`; the caller typically ignores
  > 
  > /// cache write errors (a failed write just means next run is a cache miss).
  > 
  > ///
  > 
  > /// Path: `{codex_tree_dir}/intent/.cache/{key}.json`
- `fn cache_path(codex_tree_dir: &Path, key: &str) -> std::path::PathBuf` [priv, Function] lines 92-97
  > /// Resolve the full filesystem path for a cache entry.

### `crates/codex-analyzer/src/claude_layer.rs`

- `pub fn generate_metadata_header(version: &TreeVersion) -> String` [pub, Function] lines 44-49
  > /// Returns the standard metadata comment that starts every generated file.
  > 
  > ///
  > 
  > /// Example output:
  > 
  > /// ```text
  > 
  > /// <!-- codex-tree: generation=3, delta_count=7, format_version=1.0.0 -->
  > 
  > /// ```
  > 
  > ///
  > 
  > /// In Scala: `def generateMetadataHeader(version: TreeVersion): String`
- `pub fn generate_l1(tree: &TreeStructure, modules: &[ModuleIndex], version: &TreeVersion) -> String` [pub, Function] lines 59-160
  > /// Generates the L1 markdown: a concise project summary (~500 tokens).
  > 
  > ///
  > 
  > /// Intended for Haiku-class models or any context where only the broad
  > 
  > /// shape of the project matters — entry points, key public types, quick stats.
  > 
  > ///
  > 
  > /// In Scala: `def generateL1(tree: TreeStructure, modules: Seq[ModuleIndex], version: TreeVersion): String`
- `pub fn generate_l2(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> String` [pub, Function] lines 170-272
  > /// Generates the L2 markdown: module-level detail (~2,000 tokens).
  > 
  > ///
  > 
  > /// Extends L1 with per-module symbol lists, the dependency graph, and
  > 
  > /// any design patterns from the intent layer.
  > 
  > ///
  > 
  > /// In Scala: `def generateL2(tree: TreeStructure, modules: Seq[ModuleIndex], version: TreeVersion, intent: Option[IntentOutput]): String`
- `pub fn generate_l3(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> String` [pub, Function] lines 282-408
  > /// Generates the L3 markdown: complete repository detail for Opus-class models.
  > 
  > ///
  > 
  > /// Extends L2 with every symbol (not just public), full design decisions with
  > 
  > /// rationale + confidence, and the full import/export manifest per module.
  > 
  > ///
  > 
  > /// In Scala: `def generateL3(tree: TreeStructure, modules: Seq[ModuleIndex], version: TreeVersion, intent: Option[IntentOutput]): String`
- `pub fn write_claude_layer(output_dir: &Path, l1: &str, l2: &str, l3: &str) -> std::io::Result<()>` [pub, Function] lines 419-426
  > /// Writes the three Claude layer files to `<output_dir>/claude/`.
  > 
  > ///
  > 
  > /// Creates the `claude/` subdirectory if it does not exist, then writes
  > 
  > /// `l1.md`, `l2.md`, and `l3.md`. This is the only function in this module
  > 
  > /// with I/O side effects — everything else is pure string generation.
  > 
  > ///
  > 
  > /// In Scala terms: analogous to `IO { Files.write(...) }` in cats-effect.
- `fn project_name_from_root(root: &str) -> &str` [priv, Function] lines 435-440
  > /// Extracts the final path component of the repo root as the project name.
  > 
  > ///
  > 
  > /// E.g. `/home/user/projects/codex-tree` → `"codex-tree"`.
  > 
  > /// Falls back to the full path string if there is no final component
  > 
  > /// (unlikely for any real repo root).
- `fn top_level_dirs(tree: &TreeStructure) -> Vec<&TreeEntry>` [priv, Function] lines 447-452
  > /// Returns `TreeEntry::Directory` entries whose path contains no `/` separator
  > 
  > /// after the root — i.e. direct children of the repo root.
  > 
  > ///
  > 
  > /// Depth 1 is defined as: the path contains exactly one component (no `/`).
  > 
  > /// For example `"crates"` is depth 1; `"crates/codex-parser"` is depth 2.
- `fn find_entry_points(modules: &[ModuleIndex]) -> Vec<&str>` [priv, Function] lines 458-468
  > /// Collects file paths that define a `main` function.
  > 
  > ///
  > 
  > /// A module is an entry point if it contains a symbol whose `kind` is
  > 
  > /// `Function` and whose `name` is `"main"`.
- `fn collect_key_public_types(
    modules: &[ModuleIndex],
    limit: usize,
) -> Vec<(&str, &str, Option<&str>)>` [priv, Function] lines 477-512
  > /// Collects up to `limit` key public types (structs, traits, enums).
  > 
  > ///
  > 
  > /// Sorting strategy mirrors the task spec:
  > 
  > ///   1. Symbols with a doc comment come first (more informative to the reader).
  > 
  > ///   2. Within each group the original order is preserved.
  > 
  > ///
  > 
  > /// Returns `(module_path, signature, Option<doc_comment>)` tuples.
- `fn first_sentence(doc: &str) -> &str` [priv, Function] lines 517-525
  > /// Extracts the first sentence from a doc comment (up to the first `.` or newline).
  > 
  > ///
  > 
  > /// Strips leading `/// ` or `/** */` markers before extracting.
- `mod tests` [priv, Module] lines 530-706
- `fn make_version() -> TreeVersion` [priv, Function] lines 537-556
- `fn make_tree() -> TreeStructure` [priv, Function] lines 558-584
- `fn make_modules() -> Vec<ModuleIndex>` [priv, Function] lines 586-626
- `fn metadata_header_format()` [priv, Function] lines 629-636
- `fn l1_contains_key_sections()` [priv, Function] lines 639-655
- `fn l2_extends_l1()` [priv, Function] lines 658-666
- `fn l3_extends_l2()` [priv, Function] lines 669-677
- `fn write_claude_layer_creates_files()` [priv, Function] lines 680-698
- `fn first_sentence_extraction()` [priv, Function] lines 701-705

### `crates/codex-analyzer/src/client.rs`

- `struct MessageRequest` [priv, Struct] lines 20-25
- `struct Message` [priv, Struct] lines 29-32
- `struct MessageResponse` [priv, Struct] lines 36-39
- `struct ContentBlock` [priv, Struct] lines 45-49
- `struct Usage` [priv, Struct] lines 53-56
- `pub struct ClaudeClient` [pub, Struct] lines 64-70
  > /// Stateful Claude API client.
  > 
  > ///
  > 
  > /// `tokens_used` accumulates across calls so the caller can enforce a
  > 
  > /// session-level token budget — analogous to a `Ref[IO, Int]` in Scala.
- `impl ClaudeClient` [priv, Impl] lines 72-199
- `pub fn from_env() -> Result<Self>` [pub, Function] lines 82-101 in `ClaudeClient`
  > /// Construct a client from environment variables.
  > 
  > ///
  > 
  > /// | Variable               | Required | Default                           |
  > 
  > /// |------------------------|----------|-----------------------------------|
  > 
  > /// | `ANTHROPIC_API_KEY`    | yes      | —                                 |
  > 
  > /// | `CODEX_TREE_MODEL`     | no       | `claude-sonnet-4-20250514`        |
  > 
  > /// | `CODEX_TREE_BUDGET`    | no       | unlimited                         |
  > 
  > ///
  > 
  > /// Returns `AnalyzerError::ApiKeyMissing` if the API key env-var is absent.
- `pub fn model(&self) -> &str` [pub, Function] lines 104-106 in `ClaudeClient`
  > /// The model ID in use, e.g. `"claude-sonnet-4-20250514"`.
- `pub async fn send_message(
        &mut self,
        system: &str,
        user_content: &str,
        max_tokens: usize,
    ) -> Result<String>` [pub, Function] lines 121-198 in `ClaudeClient`
  > /// Send a single-turn message to Claude and return the text response.
  > 
  > ///
  > 
  > /// POSTs to `https://api.anthropic.com/v1/messages` with the required
  > 
  > /// `x-api-key` and `anthropic-version` headers.
  > 
  > ///
  > 
  > /// Token usage is accumulated internally; if a `budget_limit` was set,
  > 
  > /// returns `AnalyzerError::BudgetExceeded` rather than making the call.
  > 
  > ///
  > 
  > /// # Errors
  > 
  > /// - `AnalyzerError::BudgetExceeded` — pre-call budget check failed
  > 
  > /// - `AnalyzerError::Http`           — network or transport error
  > 
  > /// - `AnalyzerError::ApiError`       — non-2xx HTTP status from the API
  > 
  > /// - `AnalyzerError::ResponseParse`  — response JSON doesn't contain text

### `crates/codex-analyzer/src/prompts.rs`

- `const MAX_USER_PROMPT_CHARS: usize = 30_000` [priv, Const] lines 16-16
  > /// Approximate character limit for the user prompt.
  > 
  > ///
  > 
  > /// Claude's context window is far larger, but we cap at ~30 K characters to
  > 
  > /// keep latency and cost predictable.  Characters ≈ tokens * 4 for English
  > 
  > /// prose; code is denser, so 30 K chars ≈ 7–10 K tokens.
- `pub fn intent_system_prompt() -> String` [pub, Function] lines 25-92
  > /// Return the system prompt instructing Claude to produce intent JSON.
  > 
  > ///
  > 
  > /// The prompt embeds the target JSON schema inline so Claude knows exactly
  > 
  > /// what structure to emit — analogous to passing a Scala `Decoder` definition
  > 
  > /// as documentation.
- `pub fn intent_user_prompt(modules: &[ModuleIndex]) -> String` [pub, Function] lines 102-136
  > /// Build the user message from a slice of `ModuleIndex` values.
  > 
  > ///
  > 
  > /// Each module is rendered as a compact structured summary.  If the total
  > 
  > /// content would exceed `MAX_USER_PROMPT_CHARS`, the least symbol-rich modules
  > 
  > /// are dropped to stay within budget — analogous to a Scala `takeWhile` over a
  > 
  > /// running character count.
- `pub fn format_module_summary(module: &ModuleIndex) -> String` [pub, Function] lines 144-190
  > /// Format a single `ModuleIndex` as a human-readable structured block.
  > 
  > ///
  > 
  > /// The output is intentionally compact — no JSON, just indented text — so
  > 
  > /// Claude can scan it quickly without paying the overhead of JSON delimiters.

### `crates/codex-analyzer/src/response.rs`

- `struct RawIntentOutput` [priv, Struct] lines 20-23
- `struct RawDecision` [priv, Struct] lines 26-34
- `struct RawAnchor` [priv, Struct] lines 37-40
- `struct RawPattern` [priv, Struct] lines 43-51
- `pub fn parse_intent_response(raw: &str, model: &str) -> Result<IntentOutput>` [pub, Function] lines 65-139
  > /// Parse a raw Claude response string into `IntentOutput`.
  > 
  > ///
  > 
  > /// Steps:
  > 
  > /// 1. Extract JSON from the response (strips markdown code fences if present).
  > 
  > /// 2. Deserialise into intermediate types.
  > 
  > /// 3. Clamp confidence values to `[0.0, 1.0]`.
  > 
  > /// 4. Assign sequential IDs to any item whose `id` field was empty.
  > 
  > /// 5. Wrap into the canonical output types with timestamp + model metadata.
  > 
  > ///
  > 
  > /// Returns `AnalyzerError::ResponseParse` on any failure.
- `fn extract_json(raw: &str) -> Option<&str>` [priv, Function] lines 150-178
  > /// Try to isolate a JSON object from `raw`.
  > 
  > ///
  > 
  > /// Strategy (in order):
  > 
  > /// 1. Look for a ` ```json ` code fence and extract its contents.
  > 
  > /// 2. Look for any ` ``` ` code fence.
  > 
  > /// 3. Find the first `{` and last `}` in the string.
  > 
  > /// 4. Return `None` if none of the above succeed.
- `fn current_utc_iso8601() -> String` [priv, Function] lines 184-196
  > /// Return the current UTC time as an ISO-8601 string, e.g. `"2026-03-28T12:00:00Z"`.
  > 
  > ///
  > 
  > /// Reimplemented inline (same algorithm as `codex-parser::serializer`) to avoid
  > 
  > /// a cross-crate dependency on a trivial utility.
- `fn unix_secs_to_datetime(secs: u64) -> (u64, u64, u64, u64, u64, u64)` [priv, Function] lines 201-221
  > /// Convert a Unix timestamp to `(year, month, day, hour, min, sec)`.
  > 
  > ///
  > 
  > /// Uses the proleptic Gregorian calendar (Richards' algorithm) — works past 2038.

### `crates/codex-analyzer/src/cursor_layer.rs`

- `fn cursor_usage_guide() -> &'static str` [priv, Function] lines 20-48
  > /// Markdown block inserted after the codex-tree metadata header on every level.
  > 
  > ///
  > 
  > /// Kept compact so the digest itself remains the bulk of the tokens.
- `fn wrap_for_cursor(base: &str) -> String` [priv, Function] lines 52-82
  > /// Inserts the Cursor guide immediately after the HTML metadata comment and
  > 
  > /// following blank lines, then a horizontal rule before the original body.
- `pub fn generate_cursor_l1(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
) -> String` [pub, Function] lines 85-91
  > /// L1 digest with Cursor framing (~500 tokens of structure + compact guide).
- `pub fn generate_cursor_l2(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> String` [pub, Function] lines 93-100
- `pub fn generate_cursor_l3(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> String` [pub, Function] lines 102-109
- `pub fn generate_cursor_layer_output(
    tree: &TreeStructure,
    modules: &[ModuleIndex],
    version: &TreeVersion,
    intent: Option<&IntentOutput>,
) -> crate::types::CursorLayerOutput` [pub, Function] lines 112-123
  > /// Builds all three levels (convenience for callers that want [`crate::types::CursorLayerOutput`]).
- `pub fn write_cursor_layer(output_dir: &Path, l1: &str, l2: &str, l3: &str) -> std::io::Result<()>` [pub, Function] lines 126-133
  > /// Writes `l1.md`, `l2.md`, `l3.md` under `<output_dir>/cursor/`.
- `mod tests` [priv, Module] lines 136-219
- `fn make_version() -> TreeVersion` [priv, Function] lines 143-162
- `fn make_tree() -> TreeStructure` [priv, Function] lines 164-174
- `fn make_modules() -> Vec<ModuleIndex>` [priv, Function] lines 176-199
- `fn cursor_l1_inserts_guide_and_keeps_overview()` [priv, Function] lines 202-208
- `fn write_cursor_layer_creates_files()` [priv, Function] lines 211-218

---

## Design Decisions

_Intent layer not generated._
---

## Import/Export Detail

### `crates/codex-cli/src/main.rs`

**Imports:**
- `std::path::Path` (use)
- `clap::{Parser, Subcommand}` (use)
- `colored::Colorize` (use)

### `crates/codex-cli/src/utils.rs`

**Imports:**
- `std::fs` (use)
- `std::path::{Path, PathBuf}` (use)
- `codex_parser::types::ModuleIndex` (use)
- `crate::error::Result` (use)
- `std::time::{SystemTime, UNIX_EPOCH}` (use)

**Exports:**
- `find_git_root` (Function)
- `read_all_modules` (Function)
- `format_size` (Function)
- `calculate_dir_size` (Function)
- `current_utc_iso8601` (Function)

### `crates/codex-cli/src/compaction.rs`

**Imports:**
- `std::fs` (use)
- `std::path::Path` (use)
- `codex_parser::types::{
    Delta, DeltaOperation, ModuleIndex, TreeEntry, TreeStructure, TreeVersion,
}` (use)
- `crate::error::Result` (use)

**Exports:**
- `should_compact` (Function)
- `read_deltas` (Function)
- `calculate_delta_size` (Function)
- `compact` (Function)

### `crates/codex-cli/src/error.rs`

**Imports:**
- `std::path::PathBuf` (use)

**Exports:**
- `CliError` (Enum)
- `Result` (TypeAlias)

### `crates/codex-cli/src/commands/report.rs`

**Imports:**
- `std::fs` (use)
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `codex_parser::types::{ModuleIndex, TreeVersion}` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::utils::{calculate_dir_size, find_git_root, format_size, read_all_modules}` (use)

**Exports:**
- `run` (Function)

### `crates/codex-cli/src/commands/regen.rs`

**Imports:**
- `std::fs` (use)
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `codex_parser::parser::parse_directory` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `codex_parser::serializer::{compute_stats, write_tree}` (use)
- `codex_parser::types::{TreeVersion, TreeVersionNumber}` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::git` (use)
- `crate::utils::{calculate_dir_size, current_utc_iso8601, find_git_root, format_size}` (use)
- `codex_analyzer::analyzer::{write_intent, Analyzer}` (use)
- `codex_analyzer::client::ClaudeClient` (use)
- `codex_analyzer::claude_layer` (use)
- `codex_analyzer::cursor_layer` (use)

**Exports:**
- `run` (Function)

### `crates/codex-cli/src/commands/mod.rs`

**Exports:**
- `check` (Module)
- `init` (Module)
- `regen` (Module)
- `report` (Module)
- `update` (Module)

### `crates/codex-cli/src/commands/init.rs`

**Imports:**
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `codex_parser::parser::parse_directory` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `codex_parser::serializer::{compute_stats, create_initial_version, write_tree}` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::git` (use)
- `crate::utils::{calculate_dir_size, find_git_root, format_size}` (use)
- `codex_analyzer::analyzer::{write_intent, Analyzer}` (use)
- `codex_analyzer::client::ClaudeClient` (use)
- `codex_analyzer::claude_layer` (use)
- `codex_analyzer::cursor_layer` (use)

**Exports:**
- `InitOptions` (Struct)
- `run` (Function)

### `crates/codex-cli/src/commands/check.rs`

**Imports:**
- `std::fs` (use)
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `sha2::{Digest, Sha256}` (use)
- `codex_parser::types::TreeVersion` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::git` (use)
- `crate::utils::{find_git_root, read_all_modules}` (use)

**Exports:**
- `run` (Function)

### `crates/codex-cli/src/commands/update.rs`

**Imports:**
- `std::fs` (use)
- `std::path::Path` (use)
- `colored::Colorize` (use)
- `codex_parser::diff::diff_modules` (use)
- `codex_parser::parser::parse_file` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `codex_parser::serializer::compute_stats` (use)
- `codex_parser::types::{Delta, DeltaOperation, ModuleIndex, TreeStructure, TreeVersion}` (use)
- `crate::compaction::{calculate_delta_size, compact, should_compact}` (use)
- `crate::error::{CliError, Result}` (use)
- `crate::git::{get_changed_files, get_commit_date, get_head_commit, get_uncommitted_changes}` (use)
- `crate::utils::{current_utc_iso8601, find_git_root, read_all_modules}` (use)
- `sha2::{Digest, Sha256}` (use)
- `codex_analyzer::analyzer::{write_intent, Analyzer}` (use)
- `codex_analyzer::client::ClaudeClient` (use)
- `codex_analyzer::claude_layer` (use)
- `codex_analyzer::cursor_layer` (use)

**Exports:**
- `run` (Function)

### `crates/codex-cli/src/git.rs`

**Imports:**
- `std::path::Path` (use)
- `std::process::Command` (use)
- `crate::error::{CliError, Result}` (use)

**Exports:**
- `get_head_commit` (Function)
- `get_changed_files` (Function)
- `get_uncommitted_changes` (Function)
- `is_shallow_clone` (Function)
- `get_commit_date` (Function)

### `crates/codex-parser/tests/rust_parsing.rs`

**Imports:**
- `codex_parser::parser::parse_file` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `codex_parser::types::{SymbolKind, Visibility}` (use)
- `std::path::Path` (use)

### `crates/codex-parser/tests/integration.rs`

**Imports:**
- `codex_parser::parser::parse_directory` (use)
- `codex_parser::registry::ParserRegistry` (use)
- `std::path::Path` (use)

### `crates/codex-parser/src/serializer.rs`

**Imports:**
- `std::collections::HashSet` (use)
- `std::fs` (use)
- `std::path::Path` (use)
- `std::time::{SystemTime, UNIX_EPOCH}` (use)
- `crate::error::Result` (use)
- `crate::types::{ModuleIndex, TreeStats, TreeStructure, TreeVersion, TreeVersionNumber}` (use)

**Exports:**
- `write_tree` (Function)
- `create_initial_version` (Function)
- `compute_stats` (Function)

### `crates/codex-parser/src/diff.rs`

**Imports:**
- `std::collections::HashSet` (use)
- `crate::types::{DeltaOperation, ModuleIndex}` (use)
- `super::*` (use)
- `crate::types::{Export, Import, ModuleIndex, Span, Symbol, SymbolKind, Visibility}` (use)

**Exports:**
- `diff_modules` (Function)

### `crates/codex-parser/src/types.rs`

**Imports:**
- `serde::{Deserialize, Serialize}` (use)

**Exports:**
- `SymbolKind` (Enum)
- `Visibility` (Enum)
- `Span` (Struct)
- `Symbol` (Struct)
- `Import` (Struct)
- `Export` (Struct)
- `ModuleIndex` (Struct)
- `TreeEntry` (Enum)
- `TreeStructure` (Struct)
- `TreeVersion` (Struct)
- `TreeVersionNumber` (Struct)
- `TreeStats` (Struct)
- `DeltaOperation` (Enum)
- `Delta` (Struct)

### `crates/codex-parser/src/error.rs`

**Exports:**
- `ParserError` (Enum)
- `Result` (TypeAlias)

### `crates/codex-parser/src/lib.rs`

**Exports:**
- `error` (Module)
- `language` (Module)
- `languages` (Module)
- `registry` (Module)
- `types` (Module)
- `diff` (Module)
- `parser` (Module)
- `serializer` (Module)

### `crates/codex-parser/src/languages/mod.rs`

**Exports:**
- `rust` (Module)

### `crates/codex-parser/src/languages/rust.rs`

**Imports:**
- `tree_sitter::Language` (use)
- `crate::language::LanguageAdapter` (use)
- `crate::types::{Import, Span, Symbol, SymbolKind, Visibility}` (use)

**Exports:**
- `RustAdapter` (Struct)

### `crates/codex-parser/src/parser.rs`

**Imports:**
- `std::collections::HashSet` (use)
- `std::path::Path` (use)
- `sha2::{Digest, Sha256}` (use)
- `walkdir::WalkDir` (use)
- `crate::error::{ParserError, Result}` (use)
- `crate::registry::ParserRegistry` (use)
- `crate::types::{Export, ModuleIndex, TreeEntry, TreeStructure, Visibility}` (use)

**Exports:**
- `parse_file` (Function)
- `parse_directory` (Function)

### `crates/codex-parser/src/registry.rs`

**Imports:**
- `std::collections::HashMap` (use)
- `crate::language::LanguageAdapter` (use)

**Exports:**
- `ParserRegistry` (Struct)
- `new` (Function)
- `register` (Function)
- `get_adapter` (Function)
- `languages` (Function)
- `with_defaults` (Function)

### `crates/codex-parser/src/language.rs`

**Imports:**
- `tree_sitter::Language` (use)
- `crate::types::{Import, Symbol}` (use)

**Exports:**
- `LanguageAdapter` (Trait)

### `crates/codex-analyzer/src/analyzer.rs`

**Imports:**
- `std::fs` (use)
- `std::path::Path` (use)
- `codex_parser::types::ModuleIndex` (use)
- `crate::cache` (use)
- `crate::client::ClaudeClient` (use)
- `crate::error::Result` (use)
- `crate::prompts` (use)
- `crate::response` (use)
- `crate::types::IntentOutput` (use)

**Exports:**
- `Analyzer` (Struct)
- `new` (Function)
- `analyze` (Function)
- `write_intent` (Function)

### `crates/codex-analyzer/src/types.rs`

**Imports:**
- `serde::{Deserialize, Serialize}` (use)

**Exports:**
- `Provenance` (Enum)
- `Anchor` (Struct)
- `Decision` (Struct)
- `Pattern` (Struct)
- `DecisionsDocument` (Struct)
- `PatternsDocument` (Struct)
- `IntentOutput` (Struct)
- `ClaudeLayerOutput` (Struct)
- `CursorLayerOutput` (Struct)

### `crates/codex-analyzer/src/error.rs`

**Exports:**
- `AnalyzerError` (Enum)
- `Result` (TypeAlias)

### `crates/codex-analyzer/src/lib.rs`

**Exports:**
- `analyzer` (Module)
- `cache` (Module)
- `claude_layer` (Module)
- `client` (Module)
- `cursor_layer` (Module)
- `error` (Module)
- `prompts` (Module)
- `response` (Module)
- `types` (Module)

### `crates/codex-analyzer/src/cache.rs`

**Imports:**
- `std::fs` (use)
- `std::path::Path` (use)
- `sha2::{Digest, Sha256}` (use)
- `codex_parser::types::ModuleIndex` (use)
- `crate::types::IntentOutput` (use)

**Exports:**
- `cache_key` (Function)
- `load_cached` (Function)
- `save_to_cache` (Function)

### `crates/codex-analyzer/src/claude_layer.rs`

**Imports:**
- `std::fmt::Write as FmtWrite` (use)
- `std::fs` (use)
- `std::path::Path` (use)
- `codex_parser::types::{
    ModuleIndex, SymbolKind, TreeEntry, TreeStructure, TreeVersion, Visibility,
}` (use)
- `crate::types::IntentOutput` (use)
- `super::*` (use)
- `codex_parser::types::{
        Export, Import, ModuleIndex, Span, Symbol, SymbolKind, TreeEntry, TreeStats, TreeStructure,
        TreeVersion, TreeVersionNumber, Visibility,
    }` (use)

**Exports:**
- `generate_metadata_header` (Function)
- `generate_l1` (Function)
- `generate_l2` (Function)
- `generate_l3` (Function)
- `write_claude_layer` (Function)

### `crates/codex-analyzer/src/client.rs`

**Imports:**
- `std::env` (use)
- `reqwest::Client` (use)
- `serde::{Deserialize, Serialize}` (use)
- `crate::error::{AnalyzerError, Result}` (use)

**Exports:**
- `ClaudeClient` (Struct)
- `from_env` (Function)
- `model` (Function)
- `send_message` (Function)

### `crates/codex-analyzer/src/prompts.rs`

**Imports:**
- `codex_parser::types::ModuleIndex` (use)

**Exports:**
- `intent_system_prompt` (Function)
- `intent_user_prompt` (Function)
- `format_module_summary` (Function)

### `crates/codex-analyzer/src/response.rs`

**Imports:**
- `serde::Deserialize` (use)
- `std::time::{SystemTime, UNIX_EPOCH}` (use)
- `crate::error::{AnalyzerError, Result}` (use)
- `crate::types::{
    Anchor, Decision, DecisionsDocument, IntentOutput, Pattern, PatternsDocument, Provenance,
}` (use)

**Exports:**
- `parse_intent_response` (Function)

### `crates/codex-analyzer/src/cursor_layer.rs`

**Imports:**
- `std::fmt::Write as FmtWrite` (use)
- `std::fs` (use)
- `std::path::Path` (use)
- `codex_parser::types::{ModuleIndex, TreeStructure, TreeVersion}` (use)
- `crate::claude_layer` (use)
- `crate::types::IntentOutput` (use)
- `super::*` (use)
- `codex_parser::types::{
        Export, Span, Symbol, SymbolKind, TreeEntry, TreeStats, TreeStructure, TreeVersion,
        TreeVersionNumber, Visibility,
    }` (use)

**Exports:**
- `generate_cursor_l1` (Function)
- `generate_cursor_l2` (Function)
- `generate_cursor_l3` (Function)
- `generate_cursor_layer_output` (Function)
- `write_cursor_layer` (Function)
