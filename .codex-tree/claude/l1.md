<!-- codex-tree: generation=4, delta_count=0, format_version=0.1.0 -->

# Project Overview

**codex-tree** — generation 4, 0 incremental updates

---

## Quick Stats

| Metric | Value |
|--------|-------|
| Files | 33 |
| Symbols | 227 |
| Lines of code | 5663 |
| Languages | rust |

---

## Architecture

- **crates** — 3 children, 227 symbols

---

## Entry Points

- `crates/codex-cli/src/main.rs`

---

## Key Public Types

- `pub struct Analyzer` _(in `crates/codex-analyzer/src/analyzer.rs`)_ — Orchestrates intent analysis: cache lookup → prompt build → API call →

- `pub struct ClaudeClient` _(in `crates/codex-analyzer/src/client.rs`)_ — Stateful Claude API client.
- `pub struct ClaudeLayerOutput` _(in `crates/codex-analyzer/src/types.rs`)_ — The three progressive-detail markdown documents for `.
- `pub struct CursorLayerOutput` _(in `crates/codex-analyzer/src/types.rs`)_ — The three progressive-detail markdown documents for `.
- `pub trait LanguageAdapter: Send + Sync` _(in `crates/codex-parser/src/language.rs`)_ — The core abstraction for language support — every language plugin implements this.
- `pub struct RustAdapter` _(in `crates/codex-parser/src/languages/rust.rs`)_ — Language adapter for Rust source files.
- `pub struct ParserRegistry` _(in `crates/codex-parser/src/registry.rs`)_ — Maps file extensions to language adapters.
- `pub enum AnalyzerError` _(in `crates/codex-analyzer/src/error.rs`)_
- `pub enum Provenance` _(in `crates/codex-analyzer/src/types.rs`)_
- `pub struct Anchor` _(in `crates/codex-analyzer/src/types.rs`)_
